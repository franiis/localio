{
<%   @segments.each do |term|
         term_value = term.translation
         term_key = term.key
%><%
  			if term.is_comment? == false
		 %>"<%= term_key %>": "<%= term_value %>"<% unless term == @segments.last %>,<% end %><% end  %>
<%     end %>
}